<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kamil.br"
    android:installLocation="preferExternal">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.BluetoothFinder" />
        <activity android:name=".activities.PathViewer" />
        <activity android:name=".activities.PathCreator" />
        <activity android:name=".activities.RoomCreator" />
        <activity android:name=".activities.CreateRoom" />
        <activity android:name=".activities.RoomViewer" />
        <activity android:name=".activities.RoomEditor" />
        <activity android:name=".activities.PathEditor" />
        <activity android:name=".activities.PathChooseRoom"></activity>
    </application>

</manifest>